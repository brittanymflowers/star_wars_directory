import Popup from './Popup'

describe('Popup Tests', () => {
    test('make sure species is not blank', () => {
        expect linkElement = screen.getByImg(img);
    })
    test('close button closes popup', () => {
        expect
    })
})
